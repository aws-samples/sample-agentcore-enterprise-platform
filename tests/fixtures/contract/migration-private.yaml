project: migration-private
environment: test
region: us-east-1
security:
  networking: true
migration:
  source:
    platform: ec2
    build:
      context: ./migration/simulation/existing-ec2-agent
      dockerfile: Dockerfile
    port: 8000
    invoke_path: /run
    health_path: /healthz
    trigger: http
  network:
    private_dependencies: [git.corp.internal]
    connectivity: vpn
